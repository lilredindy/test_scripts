from lettuce import step
from selenium import webdriver

@step(u'Do the random ad links test')
def do_the_random_ad_links_test(step):
    assert True, 'This step must be implemented'
	
@step(u'Do the click ad link test')
def do_the_click_ad_link_test(step):
    assert True, 'This step must be implemented'

@step(u'Do the Recently seen ads test')
def do_the_recently_seen_ads_test(step):
    assert True, 'This step must be implemented'

@step(u'Do the ad count test')
def do_the_ad_count_test(step):
    assert True, 'This step must be implemented'

@step(u'Do the share this ad test')
def do_the_share_this_ad_test(step):
    assert True, 'This step must be implemented'
